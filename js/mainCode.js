/**
window.addEventListener('resize', function() {
    if (window.innerWidth < 1400) {
        // Add classes to elements.
        var mainMenu = document.getElementById('main-menu');
        mainMenu.classList.add("mystyle");
    }
}, false);
**/